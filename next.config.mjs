/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Extend this array with any external domains you plan to load images from.
    domains: [],
  },
};

export default nextConfig;